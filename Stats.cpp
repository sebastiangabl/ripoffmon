/*
 * Stats.cpp
 *
 *  Created on: 02. Jän. 2016
 *      Author: Sebastian
 */

#include "Stats.h"

Stats::Stats(float hp, float atk, float def, float atk_sp, float def_sp, float spd, float acc, float eva) :
    HP(hp), ATK(atk), DEF(def), ATK_SP(atk_sp), DEF_SP(def_sp), SPD(spd), ACC(acc), EVA(eva) {
}
